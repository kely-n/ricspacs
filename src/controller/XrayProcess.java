package controller;

import models.Appointment;
import models.ImagingResult;

public class XrayProcess {

    /**
     * Method saves an imaging result from radiography to database and attaches it to an appointment
     * @param appointment
     * @param imagingResult
     * @return 1 if successful, 0 otherwise
     */
    public static int saveImagingResult(Appointment appointment, ImagingResult imagingResult){

        return 1;
    }


}
